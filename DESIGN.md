# pathfind — 디자인 스펙

> 레퍼런스: Glide — https://www.glide.com/
> 레퍼런스 접근일: 2026-09-11
> 레퍼런스 캡처: `research/glide/` (14장, 풀페이지 스크린샷)
> 이 문서는 레포 루트에 두며, 프로젝트가 소유하는 룩의 정본이다. Askewly 토큰을 주입하지 않는다.

## 의도 한 줄

AI 시대, 하려는 일을 한 문단 받아 → 기존 해결책·사례를 웹에서 모아 → 단계별로 배치하고 → 단계마다 "이미 있는 것 / 직접 해야 하는 것"을 가르는 공개 웹 서비스.

## 방향성

인상: 밝음, 공기감, 정리된 공백, 단 하나의 액센트(퍼플-블루), 헤드라인과 본문의 대비가 확연한 화면.

레퍼런스 근거: Glide는 밝은 오프화이트 계열 표면, 퍼플-블루 액센트 `#4f4bcb`, 헤드라인 폰트 무게 높게(GothamPro 700, 60px), 본문은 가볍고 여유(Mulish 22–30px). 섀도는 최소, 히어로 글로우는 퍼플-블루 톤 1종. 내비게이션·푸터는 간결.

우리가 그대로 쓰는 것: 밝은 표면 위주, 액센트 색 1종, 헤드라인/본문 위계 진폭, 섀도 최소 원칙, 숏 카피의 리듬감.

우리가 바꾸는 것: 서체는 한국어 환경이므로 Noto Sans KR 유지. 위계 진폭과 크기/굵기/자간만 Glide 식으로 조정. 기존 팔레트 `#4f9eff`는 버리고 `#4f4bcb` 계열로 통일.

## 컬러 (라이트 기본 + 다크 변종)

### Light (기본)

| 토큰 | 값 | 용도 |
| --- | --- | --- |
| surface | `#ffffff` | 카드·입력창·본문 배경 |
| accent | `#4f4bcb` | 링크·Primary 버튼·포커스 |
| accent-soft | `#f3f5ff` | 헤더 배경·Pill 배경 등 오프화이트 톤 |
| text | `#0a0a0a` | 헤드라인·본문 |
| text-secondary | `#5a5a5a` | 보조 텍스트 |
| border | `#e5e7eb` | 카드·입력창 테두리 |
| on-accent | `#ffffff` | accent 위 텍스트 |

쓰지 않는 색: `#4f9eff` 계열 블루. 상태 색(danger/success 등)은 텍스트·아이콘·소면적 뱃지에만. 큰 면적 배경색은 surface / accent-soft 두 종으로 제한.

### Dark (라이트의 변종, 선택)

| 토큰 | 값 | 용도 |
| --- | --- | --- |
| bg | `#0d0f12` | 페이지 배경 |
| card | `#161922` | 카드·입력창 배경 |
| text | `#e6e9ef` | 텍스트 |
| muted | `#8b95a5` | 보조 텍스트 |
| border | `#232a35` | 카드·입력창 테두리 |
| accent | `#4f4bcb` | **변경됨** — 기존 `#4f9eff` 대신 Glide와 통일 |
| accent-dim | `#2a4a7a` | accent의 어두운 변종(호버 등) |

전환: `prefers-color-scheme` 또는 상단 토글. 전환 시 색상만 바뀌고 간격·위계·형태는 동일.

## 타이포그래피

Noto Sans KR 단일 패밀리. Glide의 헤드라인/본문 대비 리듬을 한글로 옮김.

| 역할 | 크기 | 굵기 | 자간 | 행간 |
| --- | --- | --- | --- | --- |
| 헤드라인(h1) | 2.5rem | 700 | `-0.02em` | 1.15 |
| 소제목(h2) | 1.75rem | 700 | `-0.01em` | 1.2 |
| 본문 | 1rem(16px) | 400 | 0 | 1.6 |
| 보조 본문 | 1.125rem | 400 | 0 | 1.6 |
| 라벨·캡션 | 0.875rem | 500 | 0 | 1.4 |
| 힌트·푸터 | 0.8rem | 400 | 0 | 1.4 |

참고: Glide 헤드라인은 60px / 700 / 자간 좁힘(-1.8px). 한글은 크기보다 굵기와 자간으로 단단함을 표현. 헤드라인은 700 유지, 자간 약간 좁힘. 본문은 400 유지하되 크기/행간으로 여유를 준다.

## 레이아웃 & 간격

- 컨테이너 최대 너비 약 1100px, 중앙 정렬.
- 섹션 간 공백 최소 64px. 카드 내부 패딩 최소 16px.
- 헤더: 서비스명 + 상태 + 필요 시 액션 버튼. 네비게이션은 단계별 흐름에 필요한 최소한만.
- 히어로: 헤드라인 크게, 입력창·예시 버튼·시작 버튼이 한눈에.
- 결과 화면: 큰 그림 + 단계별 카드 리스트.

Glide처럼 밀도를 낮추고 호흡을 길게 가져간다.

## 고도 & 섀도

- 기본: 섀도 최소. 카드·입력창은 테두리(border)로 구분.
- 카드 섀도(선택): `rgba(40,38,74,0.05) 0px 4px 50px 0px` — Glide 실측값 기반, 약하게.
- 히어로 글로우(선택): `rgba(79,75,203,0.03) 0px 50px 150px 0px` — Glide 실측값, 퍼플-블루. 과하지 않게 필요 섹션에만.
- 포커스 링: `0 0 0 3px rgba(79,75,203,0.25)` — accent 기반.
- 장식용 그라데이션·그림자 남발 금지.

## 형태 (반경·비율)

- 버튼·입력창·Pill: 반경 6–8px 내외(소형).
- 카드: 반경 8–12px.
- 반경 종류는 화면에서 2종 이하(작게 + 보통). Pill은 상태 표시용.

## 컴포넌트 방향

- Primary 버튼: accent 배경, on-accent 텍스트, 굵기 600, 둥근 사각형.
- Secondary / Ghost: 투명 + 테두리 또는 텍스트 accent. 헤더·내비게이션 보조 액션.
- 예시 버튼(예시 답변): Ghost 캡, 선택 시 살짝 강조. 사용자가 직접 문장을 짜지 않아도 시작 가능하게.
- 상태 Pill: accent-soft 배경 + 텍스트. 준비/진행/완료 등.
- 입력창/텍스트에어리어: surface 배경, border 테두리, 라운드. 포커스 시 accent 테두리 + ring.
- 결과 카드: 단계 번호·제목·구분(이미 있는 것/직접 해야 하는 것) 포함.

## 히어로 비주얼

일러스트/사진 계열 이미지 사용 예정. 이미지 생성은 imagegen 도구 활용.
구체적인 소재·구도·색 처리는 향후 정의. 이 문서에서는 "밝은 서피스 위, accent 톤과 충돌하지 않는 정적 비주얼"으로 방향만 잡아둔다.

## 레퍼런스 캡처

Glide 디자인 언어 실측용 캡처. 이 문서의 값 근거 자료.

- 디렉터리: `glide-captures/`
- 파일: 01-landing ~ 14-resources-help (풀페이지 스크린샷 14장)
- 접근일: 2026-09-11
- 출처: https://www.glide.com/

## 하지 않는 것

- 이모지 아이콘을 쓰지 않는다.
- 헤드라인 자간을 넓히지 않는다.
- 본문 크기를 16px 미만으로 내리지 않는다.
- 한 화면에 반경 종류를 3개 넘게 쓰지 않는다.
- 채팅·AI 답변을 그대로 붙인 듯한 과한 색·섀도·그라데이션을 쓰지 않는다(askewly "AI slop을 걷어낸다" 방향성).

## 향후 정의 (미정)

- 라이트↔다크 전환 UI 위치·토글 방식
- 히어로 비주얼 소재·구도·색 처리
- 예시 버튼 문장 톤·예시 시나리오 확정
- 입문 안내·첫 화면 온보딩 문구
- 반응형 breakpoints 세부(모바일 히어로 크기 단계 등)
