

---

## 작업 흐름 (이 레포의 기본 작동 방식)

### 1) 시작 전 확인 (HERMES.md의 "시작 전 확인" 따름)

매 세션 시작 시 아래 5개를 확인한다.

1. `git status`로 dirty·untracked 확인
2. `.gitignore`가 `.env*`·`*.tmp`·`node_modules/`·`.vercel/`·`AGENTS.override.md`·`.materials/`를 막는지 확인
3. `api/*.js`에 `solar-pro4`(하이픈 없음) 모델 ID가 있는지 확인
4. `pathfind-src/` 4개 파일 무결성 확인(필요 시)
5. 비밀(`.env.local`)이 존재하면 값 말고 **존재 여부만** 확인

### 2) 작업 단위

- 한 턴에 한 가지 명확한 작업만 한다.
- 작업 시작 전 "무엇을 할지"를 MVP_WORK.md 또는 이 문서에 메모로 남긴다.
- 작업 종료 후 결과를 커밋하기 전에 검증한다(스크립트 있으면 실행, 없으면 수동 확인).

### 3) 커밋·푸시·재배포 루틴

- **커밋 단위**: 관련 있는 변경만 묶는다. 디버깅 스크립트·테스트 산출물은 커밋하지 않는다(gitignore로 막힘).
- **푸시**: `git push origin main` → Vercel 프로덕션 재배포 트리거(설정돼 있으면).
- **재배포 확인**: 프로덕션 URL에서 index 200, `/api` 200, 클라이언트 소스 키 0을 확인한다.
- **디버깅 후 정리**: 스크립트·임시 파일은 작업 종료 후 삭제한다. Git 추적에서 제거된 상태로 커밋한다.

### 4) 검증 루틴 (배포 전/후)

- ** 프론트**: index.html이 브라우저에서 열리고, 입력→인터뷰→결과 흐름이 UI 레벨에서 막히지 않는지.
- ** /api/grill**: POST `/api/grill`가 200을 반환하고 JSON 응답 구조가 예상과 맞는지 (Solar 호출 없이 구조만 볼 때는 모의 응답·로컬 점검).
- ** /api/pathfind**: POST `/api/pathfind`가 200을 반환하고 `bigPicture`, `handoffMarkdown`이 온전한지.
- ** 비밀**: 클라이언트 소스(index.html, frontend/js/*.js, api/*.js)에 키·토큰이 없는지. 응답 JSON에도 없는지.
- ** 시크릿 창**: 저장값 없는 첫 방문에서 안내가 나오고 최소 기능이 도는지.

### 5) 파일 위치 규칙

- **산출물(공개·제출)**: `index.html`, `api/*.js`, `PRD.md`, 포스터·발표자료·데모 영상(추후)
- **작업용(커밋 대상 아님 또는 별도 관리)**: `work/`(handoff·연구노트·테스트 입력), `pathfind-src/`(pathfind 원본 4개)
- **비밀**: `.env.local` (gitignore)

### 6) 모델·제약 확인

- LLM은 **Solar Pro 4**만. 코드 내 모델명 `solar-pro4`(하이픈 없음).
- 외부 LLM 호출 금지. MCP·비-LLM 공개 API는 허용(저작권·출처 표기).
- 키·토큰·프록시 주소는 클라이언트 소스·응답 JSON·PRD·커밋 어디에도 넣지 않는다.
- 매 배포는 전체 파일 세트를 다시 올린다(일부만 올리면 기존 파일이 사라짐).

---

*이 섹션은 작업이 시작되기 전에 한 번 정리하고, 루틴이 바뀌면 함께 갱신한다.*
