# JD v2 — 2026-09-12 12:50 세션 재시작용

v1(`docs/jd/`)에서 바뀐 것: 각 JD 에 「현재 상태」와 「오늘 배운 함정」 절을 넣고, 첫 과제를 파일 끝에 붙였다. 규칙은 v1 그대로.

## 쓰는 법
1. 이 폴더 4개를 `pathfind-web/docs/jd/` 에 덮어쓴다(오케스트레이터가 커밋).
2. Hermes 를 `pathfind-web` 에서 새로 연다. 순서: O → B → F. **D 는 열지 않는다**(자산 완성 뒤).
3. 각 세션 첫 메시지 = JD 파일 본문 전체. 첫 과제가 파일 끝에 있으므로 따로 붙일 것 없음.
4. 세션당 과제 하나. 과제 3~4개 지나면 새 세션(오늘 실측: 긴 세션이 초기 턴을 재생한다).
5. 라이브 검증·상태판은 Claude 세션(mabc-2026 `docs/finals-progress.md`)이 맡는다.

## 12:50 기준 공통 사실
- HEAD `2885ef3`, 작업트리 깨끗함. 라이브: `/`·`/app.html`·`/api/grill`·`/api/pathfind`·`/api/stage`·`/api/handoff` 전부 200.
- 남은 P0: **F2(실 API 연결)** → F3(랜딩) → F4(첫 방문 예시 버튼). 17:00 멘토링 최소선 = F2.
