# PRD — pathfind 웹 서비스 MVP (version 0.1)

> skill-to-service 킷 B 모드 환산, 11항목. 승인 완료(2026-09-10). 구현 착수 전 기준.

## 1) 한 줄 정의

무엇을 만들려는 사람(게임·업무 도구·문서 등 무엇이든)이, 아이디어를 명료화하는 짧은 정렬 인터뷰를 거쳐, **구현을 위한 큰 단계(기획·설계·제작·통합·테스트·출시·운영 등)와 각 단계의 할 일을 정리한 큰 그림**을 받고, 각 단계마다 **이미 있는 것(오픈소스·무료 에셋·튜토리얼·유사 사례)과 직접 해야 하는 것을 갈라 보여주는 경로**, 그리고 구현 에이전트로 이어 쓸 수 있는 handoff 문서를 받는 서비스.

## 2) 타깃 사용자

- 구체적 한 사람: 퇴직을 앞두거나 막 퇴직한 50대 후반~60대 초반, 새 일을 시작해 보려는 사람. “동네에 작은 가게를 열었는데 예약 안내 페이지가 필요하다” 같은 아이디어가 떠올랐을 때, 컴퓨터 켜고 브라우저를 열어 처음 방문해 쓴다. 스마트폰은 쓰지만 AI나 최신 도구에는 익숙하지 않고, 뭘 검색해야 하는지도 막막한 경우.
- 커버리지(무엇을 만들 때 쓸 수 있나)는 넓게 유지; MVP 데모·검증 초점은 새 일 시작 시나리오 — 작은 가게·모임·개인 기록 정리처럼 “해본 적은 없어도, 뭔가 해보려는 일”을 예시로 든다.

## 3) 문제

“무언가 새로 시작해 보고 싶다”는 생각이 들어도, 이미 비슷한 일이 어떤 식으로 이뤄지는지, 쓸 수 있는 무료 템플릿·참고 사례·쉬운 도구가 있는지 몰라서 괜히 복잡하게 시작하거나, 남들이 이미 겪은 시행착오를 다시 밟는다. 무엇보다 큰 그림과 각 단계에서 무엇을 스스로 해야 하고 무엇을 가져다 쓸 수 있는지 선택지가 보이지 않아, 생각은 있어도 첫걸음을 떼기 어렵다. 특히 컴퓨터나 최신 도구를 자주 쓰지 않던 사람이라면 “뭘 검색해야 하는지”부터 막막해서 시작 자체가 늦어진다.

## 4) 트리거

- 불러 쓰는 쪽. URL로 접속 → 첫 화면 입력 → 정렬 인터뷰(5턴 제한) → 시각화 경로 + handoff 다운로드.
- 알림·공유 링크 등으로 서비스가 먼저 가는 구조는 MVP 범위 밖. 아이디어 직후 접속 패턴이 디폴트.

## 5) 핵심 기능

**P0 (데모 필수, 최대 4개)**

1. **정렬 인터뷰** — 5턴 제한, LLM이 질문 + 추천 방향 + 예시 답변 버튼(2~3개)을 제시, 사용자는 버튼 클릭/자유 입력으로 답변. grill-me/grilling 패턴을 서비스에 흡수한 단계.
2. **큰 그림·단계 리서치** — 정렬 인터뷰 후 LLM이 구현을 위한 큰 단계(기획·설계·제작·통합·테스트·출시·운영 등)와 각 단계의 할 일을 정리한 큰 그림을 세운다. 그다음 각 단계마다 리서치를 돌리되, 병렬 가능한 건 병렬로, 순서 의존 있는 건 순차적으로 수행한다. 각 단계 리서치 결과로 “오픈소스·프레임워크가 있어서 편해질 수 있다 / 없으면 직접 규칙·성장 시스템을 정해야 한다”, “Godot vs Unity vs 이미 나온 결과물 활용” 같은 선택지를 제시한다. 필요 시 “이 작업도 pathfind로 더 좋은 방법이 있을지 re-search 하자”는 제안을 할 수 있다.
3. **경로 시각화** — 큰 단계 카드 흐름도. 각 단계 카드에 할 일, verdict(가져다 써도 됨 / 섞어야 함 / 직접 해야 함 / 선례를 못 찾음), 찾은 자료 링크, 선택지를 담는다. 프론트에서 CSS/JS로 인터랙티브 렌더링.
4. **handoff 다운로드** — 구현 에이전트로 이어 쓸 수 있는 마크다운 문서 제공. 다운로드 + 복사. 전달 대상 명칭은 일반화(“구현을 이어갈 코딩 에이전트” — 특정 제품명 미기재).

**P1 (이번에 안 하거나 최소화)**

- handoff 공유 링크(옵션 B)
- three.js 등 고급 시각화
- 주제별 특화 경질 프롬프트 다수

**다음 단계 (코어 화면 3분할 방향)**

코어 화면을 세 영역으로 나누는 방향을 검토한다: 왼쪽 출처/리서치 자료 목록, 중앙 정렬 인터뷰·대화, 오른쪽 단계 카드 마인드맵·로드맵. 이 3분할은 현재 two-pane(좌 대화 / 우 결과) 구조를 확장하는 다음 단계 옵션으로만 적어둔다.

## 6) 스킬 매핑

| pathfind SKILL.md 절 (예선 당선 스킬) | pathfind-web에서의 대응 | 비고 |
|---|---|---|
| 판정(첫 줄 판정 A/B) — 입력 대상 여부 판정 | 서비스에서는 grill 인터뷰(정렬 인터뷰)가 아이디어 명료화 역할. pathfind 본선 판정 로직은 /api에 그대로 이식되지 않음. | grill-me 흡수 |
| 조사 절차 — 단계마다 웹 검색 1회 | `api/stage.js`가 Solar 도구 호출로 웹 검색 수행(검색 공급자 추상화: SEARCH_API_URL + SEARCH_API_KEY). 머리 주석에 SKILL.md §조사 절차 원문 + 서비스화 수정 문단 수록. | 원문 이식 |
| 조사 결과 JSON 스키마(steps[], verdict, findings 등) | `api/stage.js` 출력에 verdict·findings·options·todos로 반영. 머리 주석에 SKILL.md §조사 결과 JSON 스키마 원문 + 서비스화 수정 문단 수록. `api/pathfind.js`는 bigPicture.stages(title, desc, tasks, choices, icon) 구조. | 원문 이식 |
| render.py — stdin JSON → 연구노트.md + 흐름도.html 생성, 템플릿 주입 | 웹 MVP에서는 render.py를 사용하지 않음. 큰 그림 JSON은 `api/pathfind.js`가 Solar로 직접 생성, 단계 카드는 프론트(frontend/js/render.js)가 렌더링, handoff.md는 `api/handoff.js`가 생성. | 대체 |
| 단계 분해 (단계 수 4~7, 제목 24자 이내, 실제 작업 순서) | `api/pathfind.js`가 이 절 원문을 머리 주석에 수록하고, bigPicture.stages(title·desc·tasks·choices·icon)로 반환. 단계 카드 렌더링은 프론트가 담당. | 원문 이식 |
| 응답 섹션(판정/저장결과/채팅요약/조정안내) | 서비스에서는 해당 절 구성을 그대로 쓰지 않음. handoff가 리서치 결과 전달 역할 일부 대체. | 미이식 — 판정·저장결과·채팅요약·조정안내 4섹션은 숙련 스킬의 채팅 출력 전용이라, 웹 MVP는 card 렌더링 + handoff.md 다운로드로 결과를 전달하므로 그대로 이식하지 않음 |
| 매 실행에서 할 일(웹검색+render.py+share_file) | 서비스에서는 `/api/stage` 호출(웹 검색) + 프론트 카드 렌더링 + `/api/handoff` 다운로드로 대체. | 대체 |
| 아이콘 이름 표(steps[].icon) | `api/pathfind.js`가 icon 필드를 받고, 프론트가 lucide 아이콘으로 렌더링. `api/stage.js`도 stage.icon 전달. | 이식 |

- **grill-me / grilling 패턴 (mattpocock/skills, 공개 레포)**: 정렬 인터뷰 로직. 서비스에 흡수된 단계로 쓰며, 특정 스킬명을 노출하지 않음.
- **pathfind (예선 당선 스킬)**: 단계 분해·리서치·판단 로직. 서비스에서는 /api에서 pathfind의 데이터 경로(큰 단계 + 각 단계 verdict·findings·선택지)를 이식·변형해 사용. 필요 시 서비스에 pathfind 재적용을 제안하는 흐름도 포함.

## 7) 입력·출력

**입력**

- 첫 화면: 한 문단 텍스트 박스 + 예시 프롬프트 버튼 3~4개 (예시 후보는 가게·모임·기록 정리처럼 새 일을 시작하려는 사람의 넓은 커버리지 반영)
- 정렬 인터뷰: LLM 질문에 대해 예시 버튼 클릭 또는 자유 입력. 5턴 또는 frontier 비면 종료.
- (인터뷰·리서치 중 필요 시, pathfind 재적용 제안 수락 가능)

**출력**

- **큰 그림**: 기획·설계·제작·통합·테스트·출시·운영 등 큰 단계 카드 (각 단계별 할 일 목록, verdict, 선택지, 링크)
- **단계 리서치 결과**: 각 단계마다 가져온 자료, verdict, 선택지 (예: Godot vs Unity vs 기존 결과물 활용, 오픈소스 프레임워크 유무에 따른 난이도 판단 등)
- **시각화**: 카드 흐름도로 표시 (인터랙티브, CSS/JS)
- **handoff**: 마크다운 문서 (다운로드 + 복사). 구현 에이전트로 넘길 수 있는 형태로 제공. 전달 대상 명칭은 일반화.

## 8) 필요 설정

**표 1 — 배포 연결 (Vercel 커넥터 등; 값은 취급하지 않음)**

- 정적 프론트(index.html 등) + api/*.js 서버리스 함수 구조. /api만 fetch.

**표 2 — 서비스 키 (Vercel 환경변수; 이름·용도·발급처만, 값 금지)**

| 환경변수 이름 | 용도 | 발급처 |
|---|---|---|
| `SOLAR_API_KEY` | 정렬 인터뷰(5턴) + bigPicture 생성 + 단계별 리서치 + handoff 마크다운 생성. 서버 `/api`에서만 사용. 클라이언트 소스·응답 JSON·PRD·커밋 어디에도 값 미기재. | Upstage Solar API (Upstage Console / API 발급) |
| `SEARCH_API_KEY` | 단계별 리서치에서 웹 검색 대행(Tavily 계열 기본). 서버 `/api/stage`에서만 사용. 클라이언트 소스·응답 JSON·PRD·커밋 어디에도 값 미기재. | Tavily (Tavily API 발급) |

- 실제 발급 가능 여부·구체적 값은 Vercel 환경변수 등록 시점에 확인. (값 아님; 이름·용도·발급처만 예비 기술.)
- 검색 공급자 교체 시 `api/stage.js`의 요청 형식만 바꾼다(공급자 추상화).

**표 3 — 사용자 프로필 (웹 화면 → localStorage, 재방문 시 묻지 않음, 초기화 버튼)**

- 최초 입력 텍스트, 예시 버튼 선택, 정렬 인터뷰 Q&A 누적(턴별), 재방문 시 이어서 사용 가능하게 저장. 초기화 버튼 제공.

## 9) 데이터 규칙

- 출처·기준 시각 표기 (찾은 자료의 URL·검색어·발췌 근거 포함 — pathfind 규약 계승).
- 없는 값은 ‘확인 불가’로 표시, 추정 금지.
- 키·프록시 주소는 서버(/api)에만. 클라이언트는 같은 도메인 /api만 fetch.
- 최소 권한.

## 10) 성공 기준 (데모 시나리오로 검증 가능한 조건)

**P0 기준**

- 계정·저장값 없는 시크릿 창 첫 방문에서, 첫 화면이 스스로 보이고 예시 버튼+텍스트 입력 가능.
- 예시 버튼 또는 직접 입력으로 정렬 인터뷰 진입 → 5턴 이내(또는 frontier 비면) 종료 → 큰 그림·단계 리서치 카드 + handoff 다운로드가 나옴.
- pathfind 스킬의 데이터 경로(큰 단계 + 각 단계 verdict·findings·선택지)가 서비스 /api에서 실제로 생성·사용됨.
- 클라이언트 소스에 키·프록시 주소 0.

**검증 방법 (실제 확인)**

- 프로덕션 도메인에서 index 200, 제목 문자열 원문 그대로, /api 200 직접 확인.
- 시크릿 창 테스트: 저장값 없는 첫 방문에서 안내가 스스로 나오고 최소 기능(입력→인터뷰→큰 그림/단계 리서치→결과) 동작.

## 11) 제외 범위

- 알림·공유 링크 등 서비스가 먼저 가는 기능 (MVP 밖)
- handoff 공유 링크의 외부 구현 에이전트 실제 도달 보장 (MVP 미보장)
- three.js 등 고급 시각화
- 특정 코딩 에이전트 제품명(Claude Code, Codex 등) 언급 — MVP에선 일반화로 대체
- Solar API 키 값, 그 밖의 비밀 값 (어디에도 미기재)

---

## 대응표 (걸음 ①~⑤ → PRD 항목)

| 걸음 | 사용자의 답(요약) | PRD 항목 |
|------|-------------------|----------|
| ① WHO | 20대 후반, 게임 좋아하는 비코딩 직장인, “풋볼매니저 야구 버전 만들고 싶다” 아이디어 직후 | 2) 타깃 사용자 |
| ② WHEN | 아이디어 직후, 컴퓨터 켜고 브라우저 열었을 때 첫 10분. URL 접속형, 알림·공유 링크은 MVP 밖 | 4) 트리거 |
| ③ INPUT/OUTPUT | 텍스트 박스+예시 버튼 3~4개 → 5턴 Solar 정렬 인터뷰(질문+추천+예시 버튼) → 카드 흐름도 시각화 + handoff 다운로드 | 7) 입력·출력, 5) 핵심 기능 |
| ④ SETUP | 비밀: LLM 호출 키(서버 /api 전용, 값 미기재). 프로필: localStorage(Q&A 누적 등), 초기화 버튼. 웹 검색 대행 서버. render.py → 웹 출력 로직 변환. grill-me는 서비스 흡수 단계 | 8) 필요 설정, 6) 스킬 매핑 |
| ⑤ SOLVE | 없던 때: 막연한 검색·포기·바퀴 재발명·시행착오 반복·품질 미달·시간/토큰 낭비. 있으면: 시행착오 회피, 오픈소스/에셋 활용, 아이디어 구체화, 단계별 할 일·순서 제시, handoff로 구현 에이전트 연결(일반화) | 1) 한 줄 정의, 3) 문제, 5) 핵심 기능, 11) 제외 범위 |

---

## 미해소 / 확인 필요 항목 (version 0.1)

- handoff 공유 링크(옵션 B) MVP 포함 범위 — 현재는 다운로드+복사 우선, 링크형은 후보 유지.

## 해결된 항목 (version 0.1 → 갱신)

- LLM 호출용 키 (`SOLAR_API_KEY`): 버셀에 환경변수 등록 완료. 값은 PRD·클라이언트·커밋 어디에도 미기재.
- 웹 검색 키 (`SEARCH_API_KEY`): Tavily 기준으로 버셀 환경변수 등록 완료. `/api/stage`에서만 사용.
- 큰 그림·단계 리서치 오케스트레이션 방식: `/api/pathfind.js`(큰 그림) + `/api/stage.js`(단계별 리서치, Solar 도구 호출, 검색 상한 2회) + `/api/handoff.js`(마크다운 생성)로 구성. **병렬 리서치 최적화는 아직 적용되지 않음** — 단계 순서대로 순차적으로 진행.
- render.py 웹 변환 상세: MVP에서는 render.py를 사용하지 않음. bigPicture JSON은 `api/pathfind.js`가 Solar로 직접 생성, 단계 카드는 프론트(frontend/js/render.js), handoff.md는 `api/handoff.js`가 생성.

---

## 버전·업그레이드 방침

이 PRD는 version 0.1. 구현·데모 과정에서 더 쉽고 나은 길이 발견되면 이 PRD를 수정한다(규정상 “안 읽고 만들면 엉뚱한 것 나온다” 주의는 유지 — 수정 시 A-1 걸음과 정합 재확인).

---

*승인: 2026-09-10 (skill-to-service B 모드 → C-계획 승인)*
*파일: PRD.md · 제출 규격 2쪽 내외 충족 목표 · 이 레포 루트에 위치*
